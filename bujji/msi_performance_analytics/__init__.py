"""bujji.msi_performance_analytics — Performance Analytics & Edge
Validation (Series 101).

Answers ONE question: does BUJJI actually have an edge? An OBSERVER,
never an optimiser -- this package never tunes a threshold, never
changes Strategy Selection, never fits a parameter, never uses machine
learning. It consumes ONLY real, already-recorded evidence from
Decision Auditor (Series 99) and Shadow Trading (Series 100), computes
purely descriptive statistics, and reports uncertainty honestly --
every metric that depends on sample size states that size and, below a
disclosed reliability threshold, is explicitly flagged
NOT_STATISTICALLY_RELIABLE rather than presented as a confident
conclusion."""
