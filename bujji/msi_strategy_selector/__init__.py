"""bujji.msi_strategy_selector — Strategy Selector (Series 89). Chooses
the best-fit SUITABLE strategy family by market-state matching; never
predicts direction, never optimizes against historical returns."""
